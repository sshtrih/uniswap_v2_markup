# Uniswap V2 --- что можно пропустить при индексировании

Этот документ фиксирует потенциальные действия в Uniswap V2, которые
**не покрываются только событиями `Swap`, `Mint`, `Burn`**, и которые
можно случайно пропустить при построении индексера.

------------------------------------------------------------------------

## ✅ Что полностью покрывается событиями пары

Если индексируются:

-   `Swap`
-   `Mint`
-   `Burn`

то НЕ пропускаются:

-   трейды
-   добавление ликвидности
-   удаление ликвидности

Для большинства аналитических задач этого достаточно.

------------------------------------------------------------------------

# Потенциально пропускаемые действия

## 1️⃣ Прямые ERC-20 `Transfer` в пул

### Что происходит

Кто-то может напрямую перевести токены в контракт пары:

``` solidity
token.transfer(pairAddress, amount);
```

Это:

-   ❌ НЕ вызывает `Swap`
-   ❌ НЕ вызывает `Mint`
-   ❌ НЕ вызывает `Burn`
-   ❌ НЕ вызывает `Sync`
-   ✅ вызывает только `Transfer` события у токена

Баланс токена в контракте увеличивается,\
но `reserve0` / `reserve1` остаются прежними.

Возникает расхождение:

    реальный баланс > reserve

### Когда это важно

-   реконструкция точного состояния пула
-   аудит инварианта `x * y = k`
-   исследование аномалий

### Как отслеживать

Слушать `Transfer` события токена, где:

    to == pair_address

⚠️ Существенно увеличивает объём индексируемых данных.

------------------------------------------------------------------------

## 2️⃣ `skim()`

### Что происходит

``` solidity
pair.skim(to)
```

Пул отправляет "лишние" токены (баланс − reserve).

Это:

-   ❌ НЕ вызывает `Swap/Mint/Burn`
-   ✅ вызывает только ERC-20 `Transfer`
-   ❌ НЕ изменяет `reserve`

### Когда важно

Если проводится бухгалтерская реконструкция состояния.

### Как отслеживать

Слушать `Transfer` токена, где:

    from == pair_address

------------------------------------------------------------------------

## 3️⃣ `sync()`

### Что происходит

``` solidity
pair.sync()
```

Обновляет `reserve0` и `reserve1` до текущего баланса.

Эмитится:

    Sync(reserve0, reserve1)

### Когда важно

-   восстановление точной истории резервов
-   проверка корректности состояния

### Как отслеживать

Индексировать событие:

    event Sync

------------------------------------------------------------------------

## 4️⃣ Переводы LP-токенов

LP-токен --- это ERC-20 токен самой пары.

Если LP токены переводятся:

    Transfer(from, to, amount)

Меняется владелец ликвидности.

### Когда важно

-   анализ распределения LP
-   отслеживание крупных держателей ликвидности

### Как отслеживать

Слушать `Transfer` события самого pair-контракта.

------------------------------------------------------------------------

# Уровни точности индексирования

## 🟢 Уровень 1 --- Анализ трейдов

Достаточно:

-   `Swap`
-   `Mint`
-   `Burn`

Ничего критичного не пропускается.

------------------------------------------------------------------------

## 🟡 Уровень 2 --- Корректная реконструкция резервов

Дополнительно нужно:

-   `Sync`
-   учитывать прямые `Transfer` в/из пары

------------------------------------------------------------------------

## 🔴 Уровень 3 --- Полный аудит токен-потоков

Нужно индексировать:

-   `Swap`
-   `Mint`
-   `Burn`
-   `Sync`
-   все ERC-20 `Transfer`
-   LP `Transfer`

------------------------------------------------------------------------

# Рекомендация для проекта

Если цель:

-   индексер DEX
-   анализ объёмов
-   анализ поведения LP

Достаточно:

    Swap
    Mint
    Burn

`Sync` --- опционально.\
ERC-20 `Transfer` --- не требуется.

------------------------------------------------------------------------

# Итог

Для аналитики DEX-активности ты ничего критичного не пропускаешь,\
если индексируешь только `Swap`, `Mint`, `Burn`.
